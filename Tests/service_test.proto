syntax = "proto3";

package test.service;

option go_package = "test/service/pb";

message Request {
  string query = 1;
}

message Response {
  string result = 1;
}

service TestService {
  rpc Search(Request) returns (Response);
  rpc StreamResults(Request) returns (stream Response);
}
